#Choose any name for your cluster
cluster_name = "<CLUSTER-NAME>"

#e.g: "eu-west-1"
aws_region = "<AWS-REGION>"

#Type "no" if you don't want to change kubectl current context to the newly created cluster.
connect_to_cluster = "yes"