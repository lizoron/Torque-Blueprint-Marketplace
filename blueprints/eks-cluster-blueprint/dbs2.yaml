spec_version: 2
description: Auto generated blueprint for terraform module current_user
# blueprint inputs can be provided by the user, API or CI plugin when creating an environment from this blueprint.
inputs:
  host_name:
    type: string
# blueprint outputs define which data that was generated during environment provisioning will be returned to the user, API or CI.
outputs:
  account_id:
    value: '{{ .grains.current_user.outputs.account_id }}'
  caller_arn:
    value: '{{ .grains.current_user.outputs.caller_arn }}'
  caller_user:
    value: '{{ .grains.current_user.outputs.caller_user }}'
  caller_username:
    value: '{{ .grains.current_user.outputs.caller_username }}'
grains:
  current_user:
    kind: terraform
    spec:
      source:
        store: terraform-samples
        path: current_user
      host:
      # Execution Host - the platform where the asset executable will run.
      # For example, a Kubernetes cluster can be used as an execution host.
        name: '{{ .inputs.host_name }}'
        # A service account annotated with a role ARN with permissions to run the asset
        # service-account: <service-account-name>
      inputs: []
      # The environment variables declared in this section will be available during the grain deployment as well as the grain destroy phase
      # env-vars:
      # - VAR_NAME: var value
      env-vars: []
      outputs:
      - account_id
      - caller_arn
      - caller_user
      - caller_username
      # Ansible inventory file in a YAML format
      inventory-file: ''
    # The terraform version that will be used to deploy the module
    tf-version: 1.2.4
